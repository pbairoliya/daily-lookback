# Optional legacy key (not used by default — coach runs on local Ollama).
# ANTHROPIC_API_KEY=sk-ant-...

# Prefer config.toml — copy config.example.toml and edit vault paths.
# Google: credentials.json +  uv run python google_auth.py
